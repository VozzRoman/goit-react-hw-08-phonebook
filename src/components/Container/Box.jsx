import styled from "styled-components";
import {space, layout, background, border, shadow, flexbox} from 'styled-system';

export const Box = styled('div') (space, layout, background, border, shadow, flexbox)